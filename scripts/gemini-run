#!/bin/bash
# gemini-run: Deprecated wrapper forwarding to agy-run

DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
echo "Notice: 'gemini-run' is deprecated. Forwarding to 'agy-run'..." >&2
exec "$DIR/agy-run" "$@"